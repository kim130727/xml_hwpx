[project]
name = "xml-hwpx"
version = "0.1.0"
description = "Church bulletin HWPX automation with JSON placeholders and regex cleanup"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
